{
  "id": "financial-connections-ach-direct-debit",
  "title": "Collect ACH Direct Debits",
  "description": "Collect a bank account to use ACH Direct Debit payments with account data.",
  "type": "learning",
  "settings": null,
  "steps": [
    {
      "key": "create-new-customer-chapter",
      "title": "Create a new customer",
      "description": "Create a new customer for future payments.",
      "required": true,
      "nodes": [
        {
          "type": "apiRequest",
          "key": "create-new-customer",
          "title": "Create a new customer",
          "description": "Create a new customer to associate with the payment method you save for later use.",
          "review_prompt": "Confirm the implementation calls the intended Stripe API, reuses values needed by later steps, and handles API errors without exposing secrets.",
          "request": {
            "path": "/v1/customers",
            "method": "post",
            "params": {
              "description": "customer description",
              "email": "jennyrosen@example.com",
              "name": "Jenny Rosen"
            }
          }
        }
      ]
    },
    {
      "key": "configure-payment-intent-chapter",
      "title": "Configure a Payment Intent",
      "description": "Configure a Payment Intent to accept an ACH Direct Debit payment.",
      "required": true,
      "nodes": [
        {
          "type": "apiRequest",
          "key": "payment-intent-request-account-data",
          "title": "Create a Payment Intent",
          "description": "Request access to additional bank account data from the customer.",
          "review_prompt": "Confirm the implementation calls the intended Stripe API, reuses values needed by later steps, and handles API errors without exposing secrets.",
          "request": {
            "path": "/v1/payment_intents",
            "method": "post",
            "params": {
              "customer": "${node.create-new-customer-chapter.create-new-customer:id}",
              "payment_method_options": {
                "us_bank_account": {
                  "financial_connections": {
                    "permissions": [
                      "payment_method",
                      "balances",
                      "ownership",
                      "transactions"
                    ]
                  }
                }
              },
              "payment_method_types": [
                "us_bank_account"
              ]
            }
          }
        }
      ]
    },
    {
      "key": "collect-payment-details-chapter",
      "title": "Collect Payment Details",
      "description": "Use Elements to collect a bank account for your Payment Intent.",
      "required": true,
      "nodes": [
        {
          "type": "uiComponent",
          "key": "collect-payment-details-with-elements",
          "title": "Collect Payment Details with Elements",
          "description": "Use Elements to collect a bank account for your Payment Intent.",
          "review_prompt": "Open the app and confirm the user-facing flow works as described."
        }
      ]
    }
  ]
}
