// Package docs defines the root Cobra command and all subcommands for the
// stripe docs subcommand. Commands are methods on [RootCommand], which holds
// shared dependencies injected via [WithOptions].
package docs
