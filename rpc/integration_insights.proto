syntax = "proto3";

package rpc;

option go_package = "github.com/stripe/stripe-cli/rpc";

message IntegrationInsightRequest {
  // A log ID to get the integration insight for
  string log = 1;
}

message IntegrationInsightResponse {
  // Integration insight message
  string message = 1;
}
